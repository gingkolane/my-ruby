{{!< default}}

<div class="container">
    <div class="term-tags">
        {{#tag}}
            <a class="tag tag-current" href="{{url}}">
                <span class="tag-name">{{name}}</span>
            </a>
        {{/tag}}
        {{#get "tags" limit="all" filter="name:workshop"}}
            {{#foreach tags}}
                <a class="tag" href="{{url}}">
                    <span class="tag-name">{{name}}</span>
                </a>
            {{/foreach}}
        {{/get}}
    </div>
    {{> list}}
</div>